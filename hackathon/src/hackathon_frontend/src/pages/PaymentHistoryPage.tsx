// src/pages/HistoryPage.tsx

import PaymentHistoryDashboard from '../sections/paymentHistory' // We will create this in the next step

export default function PaymentHistoryPage() {
  return <PaymentHistoryDashboard />
}